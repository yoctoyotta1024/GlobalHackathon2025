from setuptools import setup, find_packages

setup(
    name='GlobalHackathon2025',
    version='0.0.0',
    packages=find_packages(),
    install_requires=[
        'pytest',
        'sphinx',
    ],
)
